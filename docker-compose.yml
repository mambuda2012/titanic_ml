services:
  mlflow:
    build:
      context: .
      dockerfile: Dockerfile
    image: mlflow-app:latest
    container_name: mlflow_server
    ports:
      - "5001:5000"
      - "8888:8888"
    volumes:
      - ./src/models/db_models:/app/src/models/db_models
    restart: unless-stopped
